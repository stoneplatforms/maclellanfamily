# 6.3 Setup

- Build Lambda: `cd infra/lambda/sqs-consumer` → `build-linux.ps1` (or Docker)
- Terraform: `cd infra/terraform` → `terraform init` → `terraform apply`
- Set `SQS_QUEUE_URL` in Vercel
- See `infra/terraform/README.md` for deploy steps
