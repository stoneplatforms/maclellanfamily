# 6.2 Flow

- Webhook → `processWebhookFiles` → enqueue to SQS (if `SQS_QUEUE_URL` set)
- Lambda consumes SQS → downloads from Dropbox → compresses → uploads to S3
- Videos: stream to S3, MediaConvert HLS job
